[package]
name = "order-book-core"
version = "0.1.0"
edition = "2021"
license = "Apache-2.0"
authors = ["Christopher Russell"]
description = "Core types and traits for order book implementations"

[features]
default = []
tracing = ["dep:tracing"]

[dependencies]
hashbrown = "0.16.1"
itertools = "0.14.0"
tracing = { version = "0.1.44", optional = true }
num = "0.4.3"
thiserror = "2.0.18"
anyhow = "1.0.101"
